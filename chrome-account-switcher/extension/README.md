# Chrome Account Switcher

A Chrome Extension (Manifest V3) built with React, TypeScript, and Vite.

## Setup

```bash
npm install
```

## Build

```bash
npm run build
```

The production build will be generated in the `dist/` directory, ready to be loaded as an unpacked extension in Google Chrome (`chrome://extensions`).

## Development

```bash
npm run dev
```
